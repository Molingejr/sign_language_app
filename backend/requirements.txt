# Backend API (run from repo root: uvicorn backend.app.main:app)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
# Model (shared with repo)
numpy<2
torch
torchvision
opencv-python-headless
